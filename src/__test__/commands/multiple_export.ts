import { createCommand } from "../../command";

export const test1 = createCommand({
  name: "test1",
  run: () => {
  }
});

export const test2 = createCommand({
  name: "test2",
  run: () => {
  }
});
